require('mini.comment').setup()
require('mini.operators').setup({
    sort = {
        prefix = 'gs',
    },

    exchange = {
        prefix = '',
    },

    evaluate = {
        prefix = '',
    },

    multiply = {
        prefix = '',
    },

    replace = {
        prefix = '',
    },
})
require('mini.splitjoin').setup()
