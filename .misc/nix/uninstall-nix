rm -rf $HOME/{.nix-channels,.nix-defexpr,.nix-profile,.config/nixpkgs}
sudo rm -rf /etc/bashrc.backup-before-nix /etc/zshrc.backup-before-nix /etc/bash.bashrc.backup-before-nix
sudo rm -rf /nix
rm -f $HOME/.local/state/nix/profiles/home-manager*
rm -f $HOME/.local/state/home-manager/gcroots/current-home
